# We've got a global
var beanCounter = 10;

function getNumberOfItems (ordertype) {
# and we've got a local
  var beanCounter = 0
  if (ordertype == "order") {
      // do some stuff "Count?" with beanCounter...
  }
  return beanCounter;
}
# Note that the local and global variables have no effect on each other: 
# if you change one it doesn't effect on the other.  They are independent
# of variables
      
